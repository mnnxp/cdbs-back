version: '3.1'

services:
  pg:
    image: postgres:14.0
    command:
      - 'postgres'
      - '-c'
      - 'max_connections=100'
      - '-c'
      - 'shared_buffers=256MB'
      - '-c'
      - 'log_statement=all'
    environment:
      POSTGRES_PASSWORD: password
    ports:
      - 5432:5432
